[package]
name = "guitar-tuner-egui"
version.workspace = true
edition = "2021"

[dependencies]
audio-input = { path = "../audio-input" }
eframe = { version = "0.36", default-features = false, features = ["accesskit", "default_fonts", "persistence", "wgpu", "wayland", "x11"] }
egui = "0.36"
cpal = "0.18"
pitch-core = { path = "../pitch-core", default-features = false }

[dev-dependencies]
tuner-test-support = { path = "../test-support" }
