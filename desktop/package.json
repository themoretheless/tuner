{
  "name": "guitar-tuner-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "../web/node_modules/.bin/tauri",
    "dev": "../web/node_modules/.bin/tauri dev",
    "build": "cd ../web && npm run build:tauri && cd ../desktop && ../web/node_modules/.bin/tauri build",
    "build:debug": "cd ../web && npm run build:tauri && cd ../desktop && ../web/node_modules/.bin/tauri build --debug",
    "icon": "../web/node_modules/.bin/tauri icon"
  },
  "dependencies": {
    "@tauri-apps/plugin-store": "^2.4.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
