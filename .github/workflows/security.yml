name: Security Gates

# Гейты зависимостей M8: cargo-deny и npm audit.
# Zero-network проверяется в build-web после production-сборки.
# Workflow вызывается из release.yml как обязательный гейт и гоняется на каждый PR.

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  pull_request:
  workflow_call:
    inputs:
      ref:
        required: false
        type: string

permissions:
  contents: read

jobs:
  # 1. cargo-deny: лицензии, advisories (RustSec), bans, sources. deny.toml в корне.
  cargo-deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref || github.ref }}
          persist-credentials: false

      - name: Setup Rust
        uses: themoretheless/.github/.github/actions/setup-rust@2d3bc163ca57318e89e801795a0b0356d5a9d941 # first-party actions
        with:
          rust-toolchain: beta
          tools: cargo-deny@0.20.2
          cache-key: cargo-deny

      - name: cargo deny check
        run: cargo deny check

  # 2. npm audit для web/ (severity high и выше).
  npm-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.ref || github.ref }}
          persist-credentials: false

      - name: Setup Vue
        uses: themoretheless/.github/.github/actions/setup-vue@2d3bc163ca57318e89e801795a0b0356d5a9d941 # first-party actions
        with:
          node-version-file: .nvmrc
          npm-cache-dependency-path: web/package-lock.json
          working-directory: web

      - name: npm audit (web)
        working-directory: web
        # high+ = падение. Известные исключения фиксируются ТОЛЬКО через
        # overrides в package.json с комментарием, не через --audit-level=none.
        run: npm audit --audit-level=high
