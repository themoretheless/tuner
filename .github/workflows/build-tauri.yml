name: Build Tauri

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  pull_request:
  workflow_call:
    inputs:
      ref:
        required: false
        type: string

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            bundle: 'deb appimage'
          - os: macos-latest
            target: aarch64-apple-darwin
            bundle: 'dmg app'
          - os: macos-latest
            target: x86_64-apple-darwin
            bundle: 'dmg app'
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            bundle: 'nsis'

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: |
            web/package-lock.json
            desktop/package-lock.json

      - name: Setup Rust
        uses: themoretheless/.github/.github/actions/setup-rust@bb3377f2f424201379ab4e47e147f164f4014b38 # v1
        with:
          targets: ${{ matrix.target }}
          cargo-workspaces: desktop/src-tauri -> target


      - name: Install dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev libasound2-dev libudev-dev pkg-config

      - name: Install web deps
        working-directory: web
        run: npm ci

      - name: Install desktop deps
        working-directory: desktop
        run: npm ci

      - name: Build web for Tauri
        working-directory: web
        run: npm run build:tauri

      - name: Build Tauri
        working-directory: desktop
        run: npx tauri build --target ${{ matrix.target }} --bundles ${{ matrix.bundle }}
        env:
          CARGO_INCREMENTAL: 0

      - name: Upload Tauri artifacts
        uses: actions/upload-artifact@v4
        with:
          name: tauri-${{ matrix.os }}-${{ matrix.target }}
          path: |
            target/**/bundle/**/*.AppImage
            target/**/bundle/**/*.deb
            target/**/bundle/**/*.dmg
            target/**/bundle/**/*.exe
            target/**/bundle/**/*.msi
          retention-days: 7
          if-no-files-found: error
